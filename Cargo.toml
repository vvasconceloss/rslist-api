[package]
edition = "2024"
version = "0.1.0"
name = "rslist-api"
authors = ["Victor Vasconcelos"]
description = "A REST API for a to-do list developed with Rust"

[dependencies]
axum = { version = "0.8.8"}
sqlx = { version = "0.8.6", features = ["runtime-tokio", "tls-native-tls"] }
